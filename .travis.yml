language: python
python:
  - "2.7"

branch:
    only:
        - master
# command to install dependencies
before_install:
    - pip install -r requirements.txt
# command to run tests
script:
    - cd flask_report/test/
    - python splinter_tests.py
notifications:
    email:
      recipients:
        - abc549825@163.com
        - xiechao06@gmail.com
